function orderByProps () {

const obj = {
    name: 'мечник',
    health: 10,
    level: 2,
    attack: 80,
    defence: 40
};

const {name, level, attack, defence, health} = user;
}

orderByProps(obj, ["name", "level"]);