export default function orderByProps(obj, sortDirection) {
    let propArray = [];
    for (const key in obj) {
        propArray.push();
    };

    const arrResult = [];

    function sortArr (array) {
        return array.sort((a,b) => b. - a.health);
    };

    for (let i = 0; i < propArray.length; i += 1) {
        const k = propArray[i];
    }
    

    return arrResult;
}